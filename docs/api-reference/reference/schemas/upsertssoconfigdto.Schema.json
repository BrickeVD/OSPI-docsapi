{"schema":{"type":"object","properties":{"protocol":{"type":"string","enum":["OIDC","SAML"]},"enabled":{"type":"boolean","description":"Defaults to false — configure first, enable once verified."},"allowedDomains":{"example":["acme.com"],"description":"Every entry must have a currently-VERIFIED DomainVerification row owned by this organization (see POST .../domain-verifications).","type":"array","items":{"type":"string"}},"defaultRoleIds":{"description":"Role IDs granted to a JIT-provisioned first-time SSO login — at least one is required, same reasoning as InviteUserDto.roleIds.","type":"array","items":{"type":"string"}},"oidcIssuerUrl":{"type":"string","description":"Required when protocol=OIDC. The IdP's issuer URL, e.g. https://accounts.example-idp.com — must be https:// except for a loopback host (local testing only)."},"oidcClientId":{"type":"string","description":"Required when protocol=OIDC."},"oidcClientSecret":{"type":"string","description":"Required when protocol=OIDC. Shown once in the create response, never re-served on subsequent reads (matches WebhookSubscription.secret's convention)."},"samlEntryPoint":{"type":"string","description":"Required when protocol=SAML. The IdP's SSO (redirect-binding) URL."},"samlIssuer":{"type":"string","description":"Required when protocol=SAML. The IdP's entityID."},"samlIdpCertificate":{"type":"string","description":"Required when protocol=SAML. The IdP's PEM-encoded public signing certificate — public material, not a secret."},"postLoginRedirectUrl":{"type":"string","description":"Optional. When set, a successful SSO login 302s here with tokens in the URL fragment instead of returning raw JSON."}},"required":["protocol","allowedDomains","defaultRoleIds"],"title":"UpsertSsoConfigDto"},"schemaType":"response"}